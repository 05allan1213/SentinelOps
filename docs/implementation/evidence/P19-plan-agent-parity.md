# P19 AgentTool、Plan 拓扑与迁移 parity

- Status: `PASS`
- Started from: `a385087a88a8d7d03245c2c9b8fb3d3cec0247dc`；`main`；开工时工作树干净，P18 本地提交与证据均为 `PASS`
- Boundary Audit: 本单元只把六个专业 Worker 接入官方 Eino `adk.NewAgentTool`，让 P15 薄 Executor builder 通过 strict Registry 与 AgentTool 组装，并保留 `planexecute.New` 作为唯一 Plan/Executor/Replanner 总拓扑；不实现 API/Worker durable loop、Approval、Effect、Budget 或后续 MCP/Skill。
- Compatibility contract: Worker 工具名、描述、Planner 输入输出和 migration manifest 保持稳定；专业 Agent 仍由 P17/P18 的官方 `adk.ChatModelAgent` builder 提供，Executor 使用官方 session keys、max iterations 和 prompt。
- Security invariants: AgentTool 只作为 L0 编排入口；叶子 Mutation Tool 继续由 P14 RuntimeHandler fail-closed；本单元不创建 Effect、不绕过 Catalog、不新增嵌套中断协议。
- Build-or-Reuse: 复用 P15 `NewExecutorBuilder`、P17/P18 `GetDurable*Agent`、Eino `adk.NewAgentTool`、`CompositeInterrupt`/recursive cancel 传播和官方 `planexecute.New`；仅保留一个名称兼容的 Agent 委托，不创建 Runnable→Agent/Event/Resume bridge。
- Actual files: `internal/ai/agent/plan_pipeline/agent_worker.go`、`executor.go`、`plan_pipeline.go`、`p19_parity_test.go`；`internal/ai/policy/catalog.go`；`internal/ai/prompt/agents/planner.go`；`internal/service/chat/chat.go` 注释；本证据文件。
- Red test and expected failure: 先加入 P19 AgentTool/拓扑测试；第一次编译因测试使用不存在的 `adk.MessageOutput` 失败，改为官方 `adk.MessageVariant` 后，`TestPlanTopologyUsesOfficialAgentToolAndNoBridge` 按预期失败，指出生产 Worker 尚未使用 `adk.NewAgentTool`。
- Implementation: 六个稳定 Worker 名称由官方 `adk.NewAgentTool` 包装薄 `adk.Agent` 委托，延迟获取 P17/P18 的真实 ChatModelAgent；委托保留 context、AgentRunOption、streaming state，并将既有 SessionMemory 历史作为 ADK messages 注入一次。Executor 改用 P15 `NewExecutorBuilder`，基础 Tool 通过 `GetManyRequired`，总拓扑继续使用 `planexecute.New`。Framework AgentTool 在既有 Catalog 中标记为 L0/no Effect；Plan pipeline 使用现有 WorkflowRecorder 发出 canonical Agent events。Planner 明确拆分 EventAnalysis 只分析与 Intelligence `save_intelligence` 持久化职责。
- Local commands and results:
  - `go test ./internal/ai/agent/plan_pipeline ./internal/ai/agent/... -run 'Test(AgentTool|CompositeInterrupt|RecursiveCancel|MigrationParity|PlanTopology)' -count=1`: `PASS`
  - `go test ./internal/ai/agent/plan_pipeline ./internal/ai/agent/... ./internal/ai/policy ./internal/ai/tools ./internal/service/chat -count=1`: `PASS`
  - `go test -race ./internal/ai/agent/plan_pipeline ./internal/ai/agent/... ./internal/ai/policy ./internal/ai/tools -count=1`: `PASS`
  - `go test ./internal/ai/runtime -run 'Test(MutationDisabled|RecursiveCancel)' -count=1`: `PASS`
  - `go vet ./internal/ai/agent/plan_pipeline ./internal/ai/agent/... ./internal/ai/policy ./internal/ai/tools ./internal/service/chat`: `PASS`
  - `staticcheck ./internal/ai/agent/plan_pipeline ./internal/ai/policy ./internal/ai/tools`: `PASS`
  - `goimports -l <P19 Go files>`: `PASS`（无输出）
  - 生产源码负向扫描 `compose.Runnable|legacy Get*Agent|isolateCtx|ResumeWithParams|context.Background().*Run`: `PASS`（无输出）
  - `git diff --check`: `PASS`
- Additional diagnostic: `staticcheck ./internal/service/chat` 报告既存 `workflowRunCursorFromContext is unused (U1000)`；P19 只更新该包中已经过时的 AgentTool 传递说明，函数属于后续 API/Worker 接线边界，未删除、未接线，不计入 P19 局部门禁。
- Results: `PASS`
- Key assertions: 六个 Worker 的 name/description 与 migration manifest 完全一致；真实 AgentTool nested run、CompositeInterrupt、recursive cancel、streaming state 与共享 context 均通过官方 ADK 传播；Planner/Executor/Replanner 仍使用官方 `planexecute` topology/session contract。Framework AgentTool 仅为 L0 且 Effect 为空，Mutation leaves 继续返回 `POLICY_MUTATION_DISABLED`；Agent emitter 使用 P08 canonical catalog 名称。
- Deviations: 原 Worker 的自研 Runnable 执行、`isolateCtx(context.Background())`、手工 stream 聚合与手工 Agent span 被删除；这是为恢复 Eino 官方 nested state/interrupt/cancel 生命周期所需的替换。保留稳定 Worker 名称的最薄 Agent 委托，不实现 Event/Resume bridge。未执行 P20 及后续单元。
- Unfinished items: P20～P43、完整全仓门禁、E2E/Eval、在线供应商和发布动作均 `NOT RUN`，不属于 P19。
- Rollback: 仅回滚本单元本地提交，不改写历史或远程分支。
