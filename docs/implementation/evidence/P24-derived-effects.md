# P24 外部与 Derived Effect DAG

- Status: PASS
- Started from: `e6f42daa9dfd9dcfe281f332f3965472a0dc134d` (`main`, worktree clean, `origin/main` behind local HEAD by four commits)
- Spec references: `3.1`～`3.4`, `6.2`, `6.4`, `6.6`, Task 4 Effect execution order and old Ops retrofit, package boundary `9`; implementation Plan P24
- Actual files: `internal/ai/effects/{executor.go,dag.go,p24_external_test.go}`, `internal/ai/workflow/{external_effects.go,p24_external_effect_test.go}`, `internal/ai/runtime/{handler.go,interrupt.go,p24_external_effect_test.go}`, existing `internal/ai/ops/actions` and tests, existing Ops/Intelligence Tool endpoints, existing Indexer/Pipeline and regression test, P04 config/bootstrap Secret paths and tests, tracked default/Docker YAML, and this evidence file
- Red test and expected failure: PASS. The first exact filtered run failed at compile time because `buildDAG`, bounded external deadline, execution metadata, invocation classification, and the `EnsureExternalEffectDAG` / `StartExternalEffect` / `MarkExternalEffectUnknownAndPark` workflow primitives did not exist. Existing actions/indexer packages were not misreported as behavioral failures.
- Local commands:
  - `SENTINELOPS_TEST_DSN='root:***@tcp(127.0.0.1:56931)/sentinelops_p03?parseTime=true' SENTINELOPS_GOOSE_BIN=/tmp/sentinelops-p23-tools/goose go test ./internal/ai/effects ./internal/ai/workflow ./internal/ai/ops/actions ./internal/ai/indexer -run 'Test(DerivedEffect|ExternalEffect|EffectDeadline|Idempotency|UnknownWindow)' -count=1` -> FAIL as expected at compile time on the missing P24 symbols
  - `docker compose -p sentinelops-p24 -f manifest/docker/docker-compose.test.yml up -d mysql` -> PASS; isolated MySQL only, dynamically published at loopback port `56932`
  - `go test ./internal/ai/effects ./internal/ai/ops/actions ./internal/ai/indexer -run 'Test(DerivedEffect|ExternalEffect|EffectDeadline|Idempotency|UnknownWindow)' -count=1` -> PASS (exact Plan command; every package has matching tests)
  - `SENTINELOPS_TEST_DSN='root:***@tcp(127.0.0.1:56932)/sentinelops_p03?parseTime=true' SENTINELOPS_GOOSE_BIN=/tmp/sentinelops-p23-tools/goose go test ./internal/ai/effects ./internal/ai/workflow ./internal/ai/ops/actions ./internal/ai/indexer ./internal/ai/runtime ./internal/ai/tools/ops ./internal/ai/tools/intelligence ./internal/ai/tools/report ./internal/ai/tools/event ./internal/config ./internal/bootstrap ./internal/service/pipeline -count=1` -> PASS
  - `SENTINELOPS_TEST_DSN='root:***@tcp(127.0.0.1:56932)/sentinelops_p03?parseTime=true' SENTINELOPS_GOOSE_BIN=/tmp/sentinelops-p23-tools/goose go test -race ./internal/ai/runtime -run 'Test(HandlerConcurrentIsolation|ExternalEffectApprovedResume)' -count=1` -> PASS
  - `go vet ./internal/ai/effects ./internal/ai/workflow ./internal/ai/ops/actions ./internal/ai/indexer ./internal/ai/runtime ./internal/ai/tools/ops ./internal/ai/tools/intelligence ./internal/ai/tools/report ./internal/ai/tools/event ./internal/config ./internal/bootstrap ./internal/service/pipeline ./internal/dao/mysql` -> PASS
  - `rg` negative scans for Outbox/MQ/new Worker/Registry/Store and Tool-name/Effect-step switch in the Effect layer -> PASS; only the pre-existing `internal/ai/ops/actions/action.go` Registry was found
  - `rg -n 'webhook_url:' manifest/config -g '*.yaml'`; `git check-ignore -v manifest/config/config.local.yaml`; `git ls-files manifest/config/config.local.yaml` -> PASS: tracked/default YAML has no plaintext webhook field; the locally migrated complete override remains ignored and untracked
  - `git diff --check` -> PASS
  - `docker compose -p sentinelops-p24 -f manifest/docker/docker-compose.test.yml down -v --remove-orphans` -> PASS; removed only the isolated P24 MySQL container, network, and temporary volume
- Results: PASS for the exact P24 gate, external/derived workflow state tests, directly affected package regressions, RuntimeHandler race isolation, targeted vet, Secret/config validation, and architecture/source negative gates. No real provider, SMTP, Docker socket, Nginx, Embedding, or Milvus endpoint was invoked; deterministic endpoint behavior used local callbacks/`httptest`, while Indexer partial-result semantics were tested without an external service.
- Key assertions: one stable Primary plus collision-free Catalog-derived keys; parent/role/step identity excludes attempt/generation/tool_call_id; transactional Primary can start the existing `milvus_index` child; external calls receive a deadline strictly before lease expiry and provider-idempotent Webhook receives the exact effect_key; only explicit safe-not-sent errors carry retryable evidence, plain errors fail closed as unknown; succeeded endpoint + failed ledger CAS is converted to unknown handling; unknown Effect, `run.parked`, retained `active_session_key`, `effect.unknown`, and `run.parked` commit atomically; Primary success plus derived unknown never becomes overall success; succeeded results are reusable; notification/Webhook Secrets resolve only inside the final endpoint callback, temporary bytes are cleared, and values do not enter persisted/error evidence; block DB/file/reload states and Milvus partial batches are independently observable and cannot report false whole success.
- Deviations from recommended route: target repository is the Plan's calibrated `/home/monody/project/SentinelOps`; the removed historical `/home/monody/project/Fo-Sentinel-Agent` path is not used
- Raw artifact references: none
- Unfinished items: none within P24. P25 reconciliation/admin resolution and P26 Mutation cutover remain intentionally out of scope.

## Boundary Audit

- Goal: execute Catalog-fixed external and derived Effect steps with stable parent/child identity, lease-bounded deadlines, provider idempotency-key propagation, explicit send-outcome classification, partial-success evidence, succeeded reuse, and atomic `unknown` + `run.parked` persistence.
- Non-goals: P25 reconciliation claims/admin resolution, P26 global Mutation cutover and legacy direct-write removal, P27 model reliability, P42 production Gate/cutover, new Tool names, a second Worker/Store/Registry/Action adapter, MQ, Outbox, or full-suite/P43 verification.
- Compatibility contracts: preserve `policy.EffectKey(run_id, proposal_hash, effect_step)`, exact Approval/Checkpoint reauthorization, the existing Eino endpoint callback, the sole `workflow.GORMStore`, the sole `ops/actions` Registry, the existing Indexer, and legacy/ordinary HTTP behavior outside durable Effect execution.
- Safety invariants: identity excludes attempt/generation/tool_call_id; Approval, Policy, Gate, lease and deadline are revalidated immediately before every external step; external deadlines leave a lease safety margin; only a proven-not-sent failure is retryable; an ambiguous send/result window is never reported as failed/succeeded and atomically parks the Run while retaining `active_session_key`; derived failure prevents overall success; Secret values exist only inside the final endpoint callback and never enter hashes, Events, Trace, Ledger, evidence, or errors.
- Expected packages/files: existing `internal/ai/effects`, `internal/ai/workflow`, `internal/ai/runtime`, `internal/ai/ops/actions`, `internal/ai/tools/{ops,intelligence}`, `internal/ai/indexer`, P04 config/Secret tests where the existing notification configuration must be reference-only, and this evidence file.
- Verification: the exact P24 filtered gate, directly affected package tests, targeted `go vet`, and source scans proving no second Store/Registry/Worker/MQ/Outbox or Tool-name dispatch in the Effect layer.
- Rollback: revert the single P24 local commit; no Migration, remote, shared database, Compose definition, or real external endpoint is changed or invoked by this unit.

## Build-or-Reuse

| Existing SentinelOps capability | Locked Eino capability | Remaining project-specific gap | Thinnest sufficient adapter |
| --- | --- | --- | --- |
| One `workflow.GORMStore`, migrated `agent_effects`, `policy.EffectKey`, P23 Primary/derived rows, exact Approval reauthorization, P04 `UseSecret`, one `ops/actions` Registry, original Tool callbacks, Nginx helper, synchronous pipeline/Indexer entrypoints | Eino v0.9.15 `adk.InvokableToolCallEndpoint` passes the original endpoint callback through the existing `ChatModelAgentMiddleware`; official Interrupt/Resume propagates the approved Tool call, but Eino has no business Effect DAG, external outcome classifier, or MySQL parked transaction | Claim/finalize external steps outside MySQL transactions, derived ordering, deadline/lease margin, idempotency metadata, partial evidence, and ambiguous-window persistence | Extend the existing `effects.Executor` and `workflow.GORMStore`; pass stable step metadata through `context.Context` to the same original endpoint, with no Tool-name switch, endpoint Registry, queue, or copied action/indexer implementation |
