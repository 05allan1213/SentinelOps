# P18 Report/Intelligence/Ops Agent 迁移但禁写

- Status: `PASS`
- Boundary Audit: 本单元只迁移 Report、Intelligence、Ops 三个专业 Agent 到官方 Eino `adk.ChatModelAgent`，保留 Catalog 声明的 Mutation Tool 可见性，并由唯一 `RuntimeHandler` 在 endpoint 前拒绝 L1/L2；不接 Planner/AgentTool，不切换旧 Worker，不实现 Approval/Effect，不删除 Ops legacy 执行路径。
- Compatibility contract: 三个 Agent 的 name、description、instruction、模型 profile、max iterations 和 manifest inventory 与 `manifest/agent/migration-contract-v1.yaml` 保持一致；strict Registry 是唯一 Tool 来源。
- Security invariants: `POLICY_MUTATION_DISABLED` 在普通同步、普通流式、增强同步和增强流式四类 Tool endpoint 前返回；endpoint、DAO、actions.Executor、Nginx 和通知副作用计数保持为 0；不创建逐 Agent Policy wrapper。
- Build-or-Reuse: 复用 P17 的 `base.NewSpecialistAgent`、`NewSpecialistGenModelInput`、`tools.GetManyRequired` 和 P14 的无状态 `runtime.RuntimeHandler`；不新增 Registry、Runtime、Agent Loop 或 Mutation gateway。
- Expected files: `internal/ai/agent/report_pipeline`、`internal/ai/agent/intelligence_pipeline`、`internal/ai/agent/ops_pipeline`、受影响的 `internal/ai/runtime/handler_test.go`、本证据文件。
- Implementation: 三个 pipeline 新增官方 `adk.ChatModelAgent` builder、显式 `GenModelInput`、strict Catalog inventory、profile-specific model factory 和未接入 Planner 的 durable getter；legacy compose/Ops 执行路径保持不变。Report 使用 reasoning profile，Intelligence/Ops 使用 default profile。
- Tests: 三个 Agent contract test 固定 name/description/inventory；RuntimeHandler contract test 覆盖普通同步、普通流式、增强同步、增强流式四类 endpoint 以及 `BeforeAgent` 动态嵌套调用。四类 endpoint 的 endpoint counter 均为 `0`，嵌套 mutation Tool 未调用且仅产生一次已结算 Model budget。
- Local commands and results:
  - `go test ./internal/ai/agent/report_pipeline ./internal/ai/agent/intelligence_pipeline ./internal/ai/agent/ops_pipeline ./internal/ai/runtime -run 'Test(Agent|MutationDisabled)' -count=1`: `PASS`
  - `go test -race ./internal/ai/agent/report_pipeline ./internal/ai/agent/intelligence_pipeline ./internal/ai/agent/ops_pipeline ./internal/ai/runtime -run 'Test(Agent|MutationDisabled)' -count=1`: `PASS`
  - `go test ./internal/ai/agent/... ./internal/testutil/agent -count=1`: `PASS`
  - `go test -race ./internal/ai/agent/... ./internal/testutil/agent -count=1`: `PASS`
  - `go vet ./internal/ai/agent/report_pipeline ./internal/ai/agent/intelligence_pipeline ./internal/ai/agent/ops_pipeline ./internal/ai/runtime`: `PASS`
  - `staticcheck ./internal/ai/agent/report_pipeline ./internal/ai/agent/intelligence_pipeline ./internal/ai/agent/ops_pipeline ./internal/ai/runtime`: `PASS`
  - `goimports -l <P18 Go files>`: `PASS`（无输出）
  - `git diff --check`: `PASS`
  - Unfiltered affected-package `go test ./internal/ai/agent/report_pipeline ./internal/ai/agent/intelligence_pipeline ./internal/ai/agent/ops_pipeline ./internal/ai/runtime -count=1`: `FAIL` only at the existing DSN-backed `TestParkedOnOpaqueCheckpointDecodeFailure` with `SENTINELOPS_TEST_DSN is required for P12 runtime integration`; DSN-backed integration is `NOT RUN` and is outside the P18 gate.
- Results: `PASS`
- Key assertions: Report/Intelligence/Ops builder tool inventories exactly match the migration manifest and Catalog; mutation tools remain visible to the Agent but `RequireExecutable` returns `POLICY_MUTATION_DISABLED` before every wrapped endpoint and nested Tool call. No Approval/Effect implementation or P19 Planner/AgentTool wiring was added.
- Rollback: 仅回滚本单元本地提交，不改写历史或远程分支。
